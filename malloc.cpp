#include<bits/stdc++.h>
using namespace std;
int main()
{
	int *p,sum=0,len;
	cin>>len;
	p=(int*)malloc(len*sizeof(int));
	if(p==NULL)
   {  
	cout<<"memory not allocated";
	exit(0);
   }
   else
   {
   	for(int i=0;i<l;i++)
   	cin>>
   }
	
}
